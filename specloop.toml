llm_backend = "anthropic"
llm_model = "claude-sonnet-4-5"
# Cross-file RTL include dirs (packages/macros) — set per target codebase,
# e.g. ["ibex/rtl", "opentitan/hw/ip/prim/rtl"] when speccing those trees.
rtl_include_dirs = []
